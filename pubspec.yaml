name: toggleai
description: "ToggleAI Flutter/Dart SDK — feature flags, remote config, and A/B testing for Flutter apps."
version: 0.1.2
homepage: https://toggleai-docs.vercel.app
repository: https://github.com/Avijitbera/toggleai-dart.git


environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.3.0
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.9

flutter:
  # No assets or fonts needed for a pure SDK package
